// Dictates the channel the message belongs to.

// Namespace
var rp = rp || {};
rp.msgCmd = rp.msgCmd || {};
rp.msgCmd.cmdFunc = rp.msgCmd.cmdFunc || {};

rp.msgCmd.cmdFunc.channel = function(_parentMessage, _commandSelf) {
  // stub
};
